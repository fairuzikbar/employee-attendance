const WorkHour = (
    employee_id,
    employee_name,
    position_name,
    department_name,
    at_date,
    work_hour,
) => {
    return {employee_id, employee_name, position_name, department_name, at_date, work_hour}
}

module.exports = WorkHour;