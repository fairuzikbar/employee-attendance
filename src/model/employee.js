const Employee = (
    employee_id,
    employee_name,
    position_name,
    department_name,
) => {
    return {employee_id, employee_name, position_name, department_name}
}

module.exports = Employee;